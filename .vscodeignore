# Comments must be on their own line - vsce treats trailing text as part of the pattern.

.vscode/
.vscode-test/
.vscode-test.mjs
.gitignore
.yarnrc
webpack.config.js
azure-pipelines.yml
vsc-extension-quickstart.md

**/tsconfig.json
**/eslint.config.mjs
**/*.map

# Ship the compiled output in out/, not the TypeScript sources.
src/**

# src/             # <--- DO NOT IGNORE 'src' folder itself, but ignore its contents if compiled
